<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="articlens">
	<select id="selectList" parameterType="map" resultType="article">
		select * from (select rowNum rn, e.* from (select * from article where board_num=${board_num} order by article_num desc) e) where rn between ${startRow} and ${endRow}
	</select>
	<select id="total" parameterType="int" resultType="int">
		select count(*) from article where board_num=#{board_num}
	</select>
	<update id="readcount" parameterType="map">
		update article set article_read = article_read + 1 where board_num = ${ board_num} and article_num = ${ article_num}
	</update>
	<select id="select" parameterType="map" resultType="article">
		select * from article where board_num = ${ board_num} and article_num = ${ article_num}
	</select>
</mapper> 